<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.neatfaith.dhikrtracker">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AddUserActivity" />
        <activity android:name=".activity.UserDetailActivity" />
        <activity android:name=".activity.ItemTypesListActivity" />
        <activity android:name=".activity.AddItemActivity" />
        <activity android:name=".activity.AboutActivity" />
        <activity android:name=".activity.SupportActivity" />
        <activity android:name=".activity.SubItemListActivity" />
        <activity android:name=".activity.AddSubItemActivity"></activity>
    </application>

</manifest>